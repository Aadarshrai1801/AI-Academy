/**
 * Aceternity UI primitives — ported and recolored to the SaaS token system
 * (§1.3, §3).
 *
 * These are the effect vocabulary currently used by the product. Every effect
 * uses the neutral surface ramp and the single indigo accent — the colorful
 * Aceternity defaults have been deprecated in favor of the token system.
 */

// Backgrounds & textures
export { Spotlight } from "./spotlight";
export { GridBackground } from "./grid-background";
export { BackgroundBeams } from "./background-beams";

// Card effects
export { CardSpotlight } from "./card-spotlight";
export { ThreeDCard } from "./3d-card";

// Border effects
export { MovingBorder } from "./moving-border";
